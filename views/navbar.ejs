<nav class="navbar navbar-fixed-top navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button
        type="button"
        class="navbar-toggle collapsed"
        data-toggle="collapse"
        data-target="#navbar-child"
      >
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Orange</a>
    </div>
    <div class="collapse navbar-collapse" id="navbar-child">
      <form
        class="navbar-form navbar-left"
        id="hashtag-form"
        action="/post/hashtag"
      >
        <input type="text" name="hashtag" placeholder="태그 검색" />
        <button class="btn" type="submit">검색</button>
      </form>
      <div class="nav navbar-nav navbar-right" style="padding: 10px">
        <% if(user&&user.id){ %>
        <input id="my-id" type="hidden" value="<%=user.id%>" />
        <div class="dropdown" id="menu-dropdown">
          <button
            class="btn btn dropdown-toggle"
            type="button"
            id="dropdownMenu2"
            data-toggle="dropdown"
            aria-expanded="true"
          >
            메뉴
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
            <li class="btn">
              <a id="profile-update-btn">닉네임 변경</a>
            </li>
            <li class="btn">
              <a href="/myPage">내가 쓴 글 목록</a>
            </li>
            <li class="btn"><a href="/followList">팔로워 목록</a></li>
            <li class="btn">
              <a href="/likedList">좋아요 목록</a>
            </li>
            <li class="btn"><a href="/auth/logout">로그아웃</a></li>
          </ul>
          <!--프로필 변경 모달-->
          <div
            class="modal fade"
            id="profileModal"
            tabindex="-1"
            role="dialog"
            aria-hidden="true"
          >
            <div class="modal-dialog">
              <div class="modal-content">
                <div class="modal-header">
                  <button
                    type="button"
                    class="close"
                    data-dismiss="modal"
                    aria-label="Close"
                  >
                    <span aria-hidden="true">&times;</span>
                  </button>
                  <h4 class="modal-title" id="exampleModalLabel">
                    프로필 변경
                  </h4>
                </div>
                <div class="modal-body">
                  <form>
                    <div class="form-group">
                      <label class="control-label">현재 닉네임</label>
                      <%= user.nick %>
                    </div>
                    <div class="form-group">
                      <label for="modifi-nick" class="control-label"
                        >바꿀 닉네임</label
                      >
                      <input
                        type="text"
                        class="form-control"
                        id="modifi-nick"
                      />
                    </div>
                  </form>
                </div>
                <div class="modal-footer">
                  <button
                    id="profile-update"
                    type="button"
                    class="btn btn-primary"
                    onclick="setProfile()"
                  >
                    변경하기
                  </button>
                  <button
                    type="button"
                    class="btn btn-default"
                    data-dismiss="modal"
                  >
                    취소하기
                  </button>
                </div>
              </div>
            </div>
          </div>
          <!---->
        </div>
        <% }else{ %>
        <a class="btn" href="/login">로그인</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
